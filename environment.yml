name: ov_respy

channels:
  - defaults
  - conda-forge

dependencies:
  - python=3.6
  - numpy
  - pandas
  - statsmodels
  - estimagic>=0.0.9
  - pytest
  - pytest-cov
  - pytest-xdist
  - scipy
  - flake8
  - pyyaml
  - codecov
  - pip
  - pip:
      - pre-commit
      - respy
